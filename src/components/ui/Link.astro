---
interface Props {
    link: string,
    className?: string,
    variant?: 'normal' | 'inline' | 'underline',
    target?: string
}

const { link, className, variant = 'inline', target }: Props = Astro.props;

const variantClass = {
    normal: 'font-type flex items-center gap-1 decoration-transparent hover:decoration-primary-100',
    inline: 'inline decoration-primary-100 hover:decoration-primary-200',
    underline: 'flex items-center gap-1 decoration-primary-100 hover:decoration-primary-100'
}
---

<a href={link} target={target} class=`group transition duration-150 ${variantClass[variant]} underline decoration-2 select-none ${className}`>
    <slot />
</a>