---
import { Image } from "astro:assets"

interface Props {
    src: string,
    alt: string
}

const { src, alt }: Props = Astro.props;
---

<Image src={src} alt={alt} width="1600" height="900" class="w-full h-48 md:h-60 object-center object-cover">